// Example usage
let scaleComponent = {
    merged: true,
    // other properties and methods
};

if (scaleComponent.merged) {
    // This block will execute because scaleComponent.merged is true
    console.log('Scale component is merged.');
    // Other statements related to the merged state
} else {
    // Optional else block for handling cases where scaleComponent.merged is falsy
    console.log('Scale component is not merged.');
}
